# Cerebras API
LLM_API_KEY=your-cerebras-api-key-here

# Default model — used when Go does not send a model in the request (account has none set).
CEREBRAS_MODEL=llama3.1-8b

# Server
LLM_HOST=0.0.0.0
LLM_PORT=8000